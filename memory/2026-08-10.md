
## V2 confidence floor change — delta_volume_exhaustion_v2 (2026-08-10)
- Hologaun approved lowering delta_volume_exhaustion_v2 min_confidence to 0.01 (1%) to test whether low-confidence cells hold their high WR%.
- Context: Day 1 V2 analysis (analyzed_strategies_v2.md) showed inverted confidence relationship — 5-9% bucket had 73.1% global WR, driven mostly by delta_vol + magnet. magnet likes HIGH confidence (40-59%), delta_vol likes LOW (10-29%). So NO global floor change; only delta_vol lowered.
- Changes made:
  1. strategies/layer2/delta_volume_exhaustion_v2.py: MIN_CONFIDENCE 0.05 -> 0.01 (internal gate)
  2. config/strategies_v2.yaml: added min_confidence: 0.01 to delta_vol params block
  3. main.py: upgraded filter from global floor to per-strategy floor (reads min_confidence from each strat's yaml params, falls back to global 0.05)
- Pre-existing test failures (6) in test_delta_volume_exhaustion.py are UNRELATED — they test non-v2 delta_volume_exhaustion.py with broken mock Window, fail identically on baseline. No _v2 test file exists.
- This is a TEST. If noisy/bad on tomorrow's review, rollback delta_vol floor to 0.05 (one yaml line + one constant).
- Everything else untouched: magnet still 40%+, all others at global 0.05.
