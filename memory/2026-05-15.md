
## Depth Stream Removal — Experiment

**Commit:** `5ee3141 feat: remove depth subscriptions — experiment to test CPU/memory savings`
- Removed `subscribe_to_market_depth_quotes` + `subscribe_to_market_depth_aggregates`
- Removed ~730 lines of depth processing from `_on_message` (VSI, exchange flow, ESI, venue concentration, depth decay, OBI, vortex, VAMP, etc.)
- Layer2 strategies remain registered but get empty rolling windows
- `_build_depth_snapshot` returns empty dict
- **Goal:** see if CPU drops from 100% and memory drops from 14GB

## Syngex — L2 Depth Processing Restored (09:55 PDT)

The depth processing block (~730 lines) was fully restored in `_on_message()`.

**What was done:**
1. Extracted original depth block from `5ee3141^` (pre-depth-removal commit)
2. Added `_exchange_bid_sizes` / `_exchange_ask_sizes` init in `__init__`
3. Inserted depth block in `_on_message()` after quote aggression flow
4. Added `exchange_data` dict to strategy evaluation data payload

**Architecture:**
- Client-level throttle (v2.17): ~20Hz, configurable via `SYNGEX_DEPTH_HZ`
- Depth block processes throttled messages → populates rolling windows
- Rolling windows feed L2 strategies (depth_decay, depth_imbalance, exchange_flow, etc.)
- L2 strategies receive data through `exchange_data` in strategy evaluation dict

**Commit:** `c7d223d` feat: restore L2 depth processing — populate rolling windows for depth strategies
**GitHub:** pushed to `main`

**Before this fix:** Depth subscriptions were restored (v2.17) but processing block was missing → strategies got empty rolling windows → no signals.
**After this fix:** Full pipeline restored — throttled depth → rolling windows → L2 strategies fire.
