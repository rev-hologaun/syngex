# 2026-05-12 — Rev 2.01: Strategy/Code Validation, Debug & Resolution

## Summary
Completed full resolution of all 29 issues from `analysis/archon-code-review.md` across Critical (C1–C3), Significant (S1–S6), Moderate (M1–M8), and Minor (D1–D12) categories.

## Commits
| Commit | Fix | Description |
|--------|-----|-------------|
| `c6ec367` | C1 | Fix dead `depth_snapshot` — populate from stream depth data |
| `e7898b0` | C2 | Fix `duration_seconds` mutation causing infinite loop in `orb_probe.py` |
| `59a953b` | C3 | Fix duplicate logging in `engine.py` signal handler |
| `1c910ef` | S1 | Add missing `from typing import Dict` to `app_heatmap.py` |
| `b4b0bf0` | S2 | Replace blocking `while True` loops in `app_dashboard.py` |
| `469bc9a` | S4 | Implement `@enforce_read_only` decorator + `ReadOnlyError` |
| `69812ee` | S3 | Auto-initialize `_rolling_data` from `rolling_keys.ALL_KEYS` |
| `6af8c21` | S3 | Add `ROLLING_WINDOW_SIZES` mapping for non-default windows |
| `9f748bf` | S5 | Use `MappingProxyType` for immutable `Signal.metadata` |
| `cfc6704` | S6 | Add `RunningStats` class + `_load_stats_from_disk()` for O(1) lookups |
| `ad34fc6` | M1 | Remove dead `push_pair()` from `RollingWindow` |
| `9beb894` | M2 | Add `net_gamma_normalized` to `GEXCalculator.get_summary()` |
| `871e743` | M3 | Add crash recovery for phi tick accumulators |
| `4f52d38` | D5 | Add 30s timeout to TradeStationClient aiohttp session |
| `c75eb14` | D3 | Remove unused `engine.dashboard` import from `main.py` |
| `7d24d54` | — | Update tracker: mark D1–D8 as fixed |
| `ced4799` | M4 | Use env var for `HEATMAP_SECRET_KEY` |
| `7262838` | M5 | Add error handling for missing `heatmap.html` template |
| `c47608a` | M6 | Remove module-level `while True` + `st.rerun()` |
| `55f1dc0` | M8 | Replace `duration_seconds` mutation with deadline pattern |
| `d068411` | — | Mark S3/S5/S6 as fixed in tracker |
| `6bc7913` | — | Mark M4–M8 as fixed, tracker 29/29 |

## Result
**29/29 issues resolved. Zero remaining.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of like the 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**

## Orchestration Lessons — min_confidence 0.30→0.15 Conversion
- **Parallel batch spawning works:** 11 Forge subagents spawned in parallel, each handling 4 files. Completed in ~2-3 minutes total wall time instead of 20+ minutes sequential.
- **Small batches are key:** 4 files per batch is the sweet spot — small enough to verify quickly, large enough to be efficient.
- **Avoid script-based bulk edits:** Scripted replacements (via subagent) kept leaving old code bodies intact. Manual CLI intervention with precise edits was the only reliable approach for complex method replacements.
- **Always verify after each batch:** grep verification after each batch caught the one miss (iv_band_breakout.py) immediately.
- **Don't double-write memory:** When appending to memory files, verify the file hasn't been corrupted by previous writes. Append-only policy is correct but requires careful file I/O.
- **Subagent completion is push-based:** Don't poll subagent status in loops. Wait for completion events. If a completion arrives after your final answer, reply NO_REPLY.
- **41 files × 13 batches = proven pattern for future large-scale changes.**
